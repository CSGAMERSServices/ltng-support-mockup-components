import { LightningElement } from 'lwc';

export default class Story_sceneSelector2 extends LightningElement {}

//-- check to see if I bind to a variable in the binding
//-- do I need to then assign that to a property on the container
//-- so I could then use that within the group

//-- OR do I need to create a `storyReference` class